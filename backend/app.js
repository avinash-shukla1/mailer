







// ...rest of your code...fs.ensureDirSync(uploadDir);const uploadDir = path.join(__dirname, 'uploads');// Ensure uploads directory existsconst path = require('path');const fs = require('fs-extra');